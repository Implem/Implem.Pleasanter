case "#ColumnName#":
    line = line.Replace(
        "[#ColumnName#]",
        allowed
            ? #modelName#Model.#ColumnName#.ToExport(
                context: context,
                column: column)
            : string.Empty);
    break;